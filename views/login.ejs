<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login · TinyLink</title>
  <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
  <div class="auth-wrapper">
    <div class="auth-header">
      <div class="auth-badge">
        <span>🔐 Secure Access</span>
      </div>
      <h1 class="auth-title">Welcome back to TinyLink</h1>
      <p class="auth-subtitle">Sign in to manage your short links and view stats.</p>
    </div>

    <% if (error) { %>
      <div class="auth-error"><%= error %></div>
    <% } %>

    <form class="auth-form" action="/login" method="POST">
      <div class="auth-field">
        <label class="auth-label">Email</label>
        <input
          class="auth-input"
          type="email"
          name="email"
          value="<%= values.email || '' %>"
          placeholder="you@example.com"
          required
        >
      </div>

      <div class="auth-field">
        <label class="auth-label">Password</label>
        <input
          class="auth-input"
          type="password"
          name="password"
          placeholder="••••••••"
          required
        >
      </div>

      <button type="submit" class="auth-button">
        Log in
      </button>

      <p class="auth-hint">
        Hint: Use the email & password you created on the sign up page.
      </p>
    </form>

    <div class="auth-footer">
      <span>New to TinyLink?</span>
      <a href="/signup">Create an account</a>
    </div>
  </div>
</body>
</html>
